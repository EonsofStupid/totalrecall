# RETIRED — wrong split

This repository sold a second engine ("TotalRecall") beside Connectome.
That split is **void**.

Connectome is **one engine and one tool**: Surreal graph/documents/relations
with Surreal's native vector path replaced in-process by Qdrant `segment`
tech (`DEFINE INDEX … QORTEX`). Stdio / signals on the face.

Do not depend on this repo. Do not recreate it.
The fused tree is https://github.com/EonsofStupid/surrealdb at `03e44e2`.
