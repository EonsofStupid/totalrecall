[toolchain]
# Pin to stable 1.96 — uses cfg_select, vec_into_raw_parts, ptr_as_ref_unchecked
# (stabilized in 1.96). Pinned for reproducible builds; stable so Clyffy can
# consume TotalRecall (trecall) on the same stable toolchain.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
